<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parvatihelix">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShippingAddress"></activity>
        <activity android:name=".TransactionStructure" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".MyNavigation"
            android:label="@string/title_activity_my_navigation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".TransactionActivity" />
        <activity android:name=".Items" />
        <activity android:name=".FillDetails" />
        <activity android:name=".productdetails" />
        <activity android:name=".Cart" />
        <activity android:name=".Login" />
        <activity android:name=".MainPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="rzp_test_mvbTi0AATwkQRv" />
    </application>

</manifest>